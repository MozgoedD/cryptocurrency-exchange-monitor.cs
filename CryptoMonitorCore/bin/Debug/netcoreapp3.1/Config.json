{
  // параметры: название монеты 1/
  // название монеты 2 (монета 1 + монета 2 = символE)

  "Symbols": [
    [ "XLM", "USDT" ],
    [ "XLM", "BTC" ],
    [ "XLM", "ETH" ],

    [ "NPXS", "BTC" ],
    [ "NPXS", "ETH" ],


    [ "ATP", "USDT" ],

    [ "BTS", "USDT" ],
    [ "BTS", "BTC" ],

    [ "PAY", "USDT" ],
    [ "PAY", "ETH" ],

    [ "THETA", "USDT" ],
    [ "THETA", "ETH" ],

    [ "RUFF", "USDT" ],
    [ "RUFF", "BTC" ],
    [ "RUFF", "ETH" ],

    [ "NOT", "EXIST" ]
    // [ "ATOM", "BTC" ],
    // [ "BCH", "BTC" ],
    // [ "EOS", "BTC" ],
    // [ "ETC", "BTC" ],
    // [ "ETH", "BTC" ],
    // [ "LTC", "BTC" ],
    // [ "XTZ", "BTC" ],
    // [ "ONT", "BTC" ],

    // [ "BTC", "USDT" ],
    // [ "ONT", "USDT" ],
    // [ "XPO", "USDT" ],
    // [ "TT", "USDT" ],
    // [ "BCH", "USDT"],
    // [ "LTC", "USDT"],

    // [ "EOS", "ETH"],
    // [ "ATOM", "ETH"],
    // [ "ONT", "ETH" ],
    // //////////////

    // [ "SNX", "USDT" ],
    // [ "SNX", "BTC" ],
    // [ "SNX", "ETH" ],

    // [ "AKRO", "USDT" ],
    // [ "AKRO", "BTC" ],
    // [ "AKRO", "ETH" ],

    // [ "BAX", "USDT" ],
    // [ "BAX", "BTC" ],
    // [ "BAX", "ETH" ],

    // [ "VRA", "USDT" ],
    // [ "VRA", "BTC" ],
    // [ "VRA", "ETH" ],

    // [ "NWC", "USDT" ],
    // [ "NWC", "BTC" ],
    // [ "NWC", "ETH" ],

    // [ "DAG", "USDT" ],
    // [ "DAG", "BTC" ],
    // [ "DAG", "ETH" ],

    // [ "AMPL", "USDT" ],
    // [ "AMPL", "BTC" ],
    // [ "AMPL", "ETH" ],

    // [ "KSM", "USDT" ],
    // [ "KSM", "BTC" ],
    // [ "KSM", "ETH" ]
  ],
  "Urls": {
    "gateio": "wss://ws.gate.io/v3/",
    "okex": "wss://real.OKEx.com:8443/ws/v3",
    "huobi": "wss://api.huobi.pro/ws"
  },
  "General": {
    "minDiff": "0.05",
    "minStep": "0.01",
    "lotMinUsd": "3"
  }
}
